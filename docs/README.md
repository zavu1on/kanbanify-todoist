# Kanbanify Todoist Docs

**Kanbanify Todoist** — desktop-приложение (Electron), UI-клиент для Todoist, который добавляет kanban-статусы для задач через встроенные теги. Приложение опирается только на функционал бесплатного тарифа Todoist.

Текущий этап — проектирование, код ещё не написан.


## Файловая структура

```
docs/
├── README.md                  — навигационный хаб (этот файл)
├── SPECIFICATION.md           — техническое задание: экраны, сценарии, поведение
└── decisions/                 — ADR (Architecture Decision Records)
    ├── README.md              — паттерн, процесс и шаблон ADR
    └── 00-use-adr.md          — использование паттерна ADR
```


## Навигация

| Хочу найти | Файл |
|---|---|
| Что система должна *уметь делать* — экраны, сценарии, поведение | [`SPECIFICATION.md`](SPECIFICATION.md) |
| Термины и сущности — Задача, Проект, Лейбл, Kanban-статус, Срок, Приоритет | [`SPECIFICATION.md`](SPECIFICATION.md), раздел «Доменная модель» |
| Что именно нельзя использовать из-за бесплатного тарифа Todoist | [`SPECIFICATION.md`](SPECIFICATION.md), раздел «Ограничения тарифа» |
| Как устроен вход по токену и где он хранится | [`SPECIFICATION.md`](SPECIFICATION.md), раздел «Авторизация» |
| Почему было принято то или иное архитектурное решение | [`decisions/`](decisions/) |
| AI-инфраструктуру проекта (скиллы, команды, MCP, агенты) | [`../CLAUDE.md`](../CLAUDE.md) |
| Правила форматирования этой документации | Раздел ниже в этом файле |


## Процесс ADR

Описание паттерна, процесс создания решений и шаблон файла — в [`decisions/README.md`](decisions/README.md).


## Правила форматирования

- Документация пишется на русском языке
- Между параграфами любого уровня отступ в две строки
- Все определения выделяются **полужирным**. Определения бизнес-сущностей дополнительно переводятся на английский, как они будут прописываться в коде
- При упоминании бизнес-сущностей в предложении они выделяются **полужирным**, а также пишутся с большой буквы
- Название полей выделяется <u>подчёркиванием</u>
- Акцент на слове или использование слова в переносном смысле задаётся *курсивом*
- Между элементами списка могут как идти пропуски в строку (для всех элементов), так их может и не быть (также для всех элементов)


## Актуализация навигационного хаба

Этот файл — единственный источник маршрутизации по `docs/`. При добавлении, удалении или изменении сути (не опечатки) любого файла в `docs/` — обнови таблицу **Навигация** и дерево **Файловая структура** в этом же коммите. Тот же принцип действует для AI-обвязки: см. раздел «Актуализация» в [`../CLAUDE.md`](../CLAUDE.md).
